version: "2"
services:
  qarate-test:
    build: .
    command: tests/catalog/search.js
    environment:
      - TEST=test
    volumes:
      -  ./reports:/root/reports
      -  ./screenshots:/root/screenshots
